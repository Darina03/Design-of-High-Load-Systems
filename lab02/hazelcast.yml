hazelcast:
  cluster-name: lab02-cluster
#  properties:
#    hazelcast.logging.type: none
  network:
    port:
      auto-increment: false
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: true
        member-list:
          - hz1:5701
          - hz2:5701
          - hz3:5701

  cp-subsystem:
    cp-member-count: 3
    group-size: 3